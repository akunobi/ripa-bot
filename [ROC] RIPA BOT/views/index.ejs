<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warning Log</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; background-color: #2c2f33; color: #ffffff; padding: 20px; }
        h1 { color: #7289da; border-bottom: 2px solid #7289da; padding-bottom: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { padding: 12px 15px; text-align: left; border-bottom: 1px solid #4f545c; }
        th { background-color: #40444b; }
        tr:nth-child(even) { background-color: #36393f; }
        tr:hover { background-color: #4f545c; }
        .container { max-width: 1200px; margin: auto; background-color: #23272a; padding: 20px; border-radius: 8px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>📜 Warning Log</h1>
        <table>
            <thead>
                <tr>
                    <th>Date</th>
                    <th>User</th>
                    <th>User ID</th>
                    <th>Tag</th>
                    <th>Moderator</th>
                </tr>
            </thead>
            <tbody>
                <% if (warnings.length > 0) { %>
                    <% warnings.forEach(warning => { %>
                        <tr>
                            <td><%= new Date(warning.timestamp).toLocaleString() %></td>
                            <td><%= warning.username %></td>
                            <td><%= warning.userId %></td>
                            <td><%= warning.tag %></td>
                            <td><%= warning.moderator %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="5" style="text-align: center;">No warnings have been logged.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
